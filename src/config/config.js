const CONFIG = {
    ASSETS_PATH: '/assets/', // Base path for your static assets
    BASE_API_URL: 'https://deijobs.in/deijobs-api/api/', // Base URL for your API requests
    // Add other configuration constants as needed
  };
  
export default CONFIG;